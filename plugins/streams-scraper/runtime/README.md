This runtime currently owns the scraper settings UI.

The stream sidebar, playback resolution, and provider-specific fetch pipeline
remain in Lumio core until they can be migrated behind stable stream-provider
SDK contracts without risking existing stream settings or playback behavior.
